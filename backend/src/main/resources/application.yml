server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/kgms?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Cjb77xxh!
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 400MB
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hjc.kgms.model.entity
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    secret: KGMS_JWT_SECRET_CHANGE_ME_2026_03_12_KGMS
    expire-seconds: 86400
  storage:
    provider: local
    local-base-path: ./storage
    local-public-path: /storage
    oss:
      endpoint: https://oss-cn-hangzhou.aliyuncs.com
      bucket: your-bucket-name
      access-key-id: your-access-key-id
      access-key-secret: your-access-key-secret
      base-url: https://your-bucket-name.oss-cn-hangzhou.aliyuncs.com
